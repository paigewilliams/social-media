<h1>Hello</h1>
<ul>
  <% @links.each do |link| %>
  <li><%= link.url %>, <%= link.rating %>

    <!-- <%= form_tag edit_link_path(link.id) do %>
      <%= submit_tag '+' %>
    <% end %> -->
    <%= link_to "+", edit_link_path(link) %>


      <!-- <%= button_to "+", edit_link_path(link) %> -->



  </li>
  <% end %>
</ul>
